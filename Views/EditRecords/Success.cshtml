@{
    ViewData["Title"] = "Success";
}

<h1>Успех!</h1>
<div>
    <form method="post">
        <button type="submit" formaction="@Url.Action("Index", "EditRecords")" class="btn btn-primary">Редактировать другие записи</button>
    </form>
</div>
